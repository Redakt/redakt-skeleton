@inherits RedaktPage<RedaktSkeleton.ViewModels.Pages.ContentPage>
@attribute [DisplayName("Content Page")]
@{
    Layout = "Shared\\_Layout.cshtml";
}
@foreach (var component in Model.Content.Components)
{
    @await Html.PartialAsync("~/Components/_" + component.GetType().Name + ".cshtml", component)
}
